(function () {
	describe('RealTime test suite', function () {

		describe('General API', function () {
			it('should be defined', function () {
				expect(window.WisemblyRealTime).to.be.a('function');
			});
		});

	});
}());
